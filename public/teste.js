            // let elementDiv   = document.createElement('div')
            // let elementInput = document.createElement('input')

            // let elementpName     = document.createElement('p')
            // let elementpDelivery = document.createElement('p')
            // let elementpValue    = document.createElement('p')


            // elementpNameText      = document.createTextNode(entrega.nome)
            // elementpDeliveryText  = document.createTextNode('0')
            // elementpValueText     = document.createTextNode('R$0,00')
            
            // elementInput.setAttribute('class', 'input-value')
            // elementInput.setAttribute('id', entrega.nome)
            // elementpValue.setAttribute('class', 'value')
            // elementpValue.setAttribute('id', `p${entrega.nome}`)
            
            

            // elementpName.appendChild(elementpNameText)
            // elementpDelivery.appendChild(elementpDeliveryText)
            // elementpValue.appendChild(elementpValueText)

            // elementDiv.appendChild(elementInput)
            // elementDiv.appendChild(elementpName)
            // elementDiv.appendChild(elementpDelivery)
            // elementDiv.appendChild(elementpValue)

            // divInputs.appendChild(elementDiv)

            var numbers = [4, 2, 5, 1, 3];
            numbers.sort(function(a, b) {
                    return a - b;
                });
            console.log(numbers);